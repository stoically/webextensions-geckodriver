{
  "name": "example",
  "version": "0.0.0",
  "description": "Example",
  "devDependencies": {
    "mocha": "^5.0.5",
    "webextensions-geckodriver": "^0.7.0"
  },
  "scripts": {
    "test": "mocha test/**/*.test.js --timeout 10000"
  }
}
